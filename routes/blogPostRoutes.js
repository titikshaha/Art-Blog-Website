// backend/routes/blogPostRoutes.js

const express = require('express');
const router = express.Router();
const BlogPost = require('../models/BlogPost');

// Define routes for CRUD operations (similar to artworkRoutes.js)
